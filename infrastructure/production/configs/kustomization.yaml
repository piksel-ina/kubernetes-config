apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cluster-issuers.yaml - managed by Terraform (terraform-iac/modules/cluster-addons/)
  # storage-class.yaml - managed by Terraform (terraform-iac/modules/cluster-addons/)
  # db users + db-endpoint - managed by Terraform (terraform-iac/modules/applications/)
  - rds-ca-cert.yaml
  - notifications.yaml
