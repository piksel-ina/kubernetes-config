apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tileserver

resources:
  - ../../base/tileserver
  - ingress.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: tileserver
